<%= form_for(@order_item_status) do |f| %>
  <% if @order_item_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_item_status.errors.count, "error") %> prohibited this order_item_status from being saved:</h2>

      <ul>
      <% @order_item_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
